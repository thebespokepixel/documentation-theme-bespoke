<section class="py0 px3 clearfix note">
	<h2 id="<%- kebabCase(note.namespace) %>" class="m0">
		<%- note.name %>
	</h2>
	<% if (note.description) { %>
		<%= md(note.description) %>
	<% } %>
	<% if (note.example) { %>
		<%= md(usage(note.example)) %>
	<% } %>
</section>
