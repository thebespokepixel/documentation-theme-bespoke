<section class="py0 px2 clearfix note">
	<h3 id="<%- kebabCase(note.namespace) %>" class="m0">
		<%- note.name %>
	</h3>
	<% if (note.description) { %>
		<%= md(note.description) %>
	<% } %>
	<% if (note.example) { %>
		<%= md(usage(note.example)) %>
	<% } %>
</section>
